package simulation.js;

/**
 * The simulation of a functional interface with TWO parameters and VOID return
 * value
 *
 * @param <T>
 * @param <S>
 * @author gianpiero.diblasi
 */
@FunctionalInterface
public interface $Apply_2_Void<T, S> {

  void $apply(T elementT, S elementS);
}
